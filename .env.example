# Plant.id API Configuration
# Get your API key from: https://admin.kindwise.com/
EXPO_PUBLIC_PLANT_ID_KEY=your_api_key_here

# Quiz Configuration
# Set minimum required scans to access quiz (default: 1)
# Set to 0 or lower to allow quiz access regardless of scan count
EXPO_PUBLIC_QUIZ_MIN_SCANS=1
